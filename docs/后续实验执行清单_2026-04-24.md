# 后续实验执行清单

适用日期：`2026-04-24`

适用仓库：`/home/dkj/research/uav_dt_project`

目标：
在当前已经完成 `paper_base / paper_hard` 定向优化验证的基础上，补齐论文所需的正式实验链条，形成一版可用于写作与投稿的完整证据包。

---

## 1. 当前状态快照

已完成：

1. `rollout_joint` 核心逻辑调优
   - `AoI discount`
   - `sync request / low-bandwidth` 惩罚
   - `VOI bonus`
   - `resync cooldown`
   - `paper_base` 场景专用 horizon 调整

2. `paper_base` 定向 20-seed 对比
   - 输出目录：
   `results/paper_base_multiseed_2026-04-24_rollout_tuned`

3. `paper_hard` 定向 20-seed 对比
   - 输出目录：
   `results/paper_hard_multiseed_2026-04-24_rollout_tuned`

4. GitHub 同步
   - 仓库：
   `DuKeju1101/uav_dt_project`

还未正式完成：

1. 最新口径的完整主表
2. 正式消融实验
3. runtime tradeoff
4. 随机扰动泛化实验
5. 与最新主表同步的 holdout / pipeline 文档

---

## 2. 优先级总览

### P0 必须完成

1. 最新主表重跑
2. 主方法 paired comparison 结果固化
3. 正式消融实验

### P1 强烈建议完成

1. runtime tradeoff
2. 随机扰动泛化实验
3. 最新口径 holdout / pipeline 文档更新

### P2 加分项

1. toy 小场景理论上界
2. coupling / threshold 分析图
3. publication suite / 补充图表整理

---

## 3. 实验清单

## A. 最新主表重跑

优先级：`P0`

目的：
用当前调优后的 `rollout_joint` 重新生成统一口径主表，回答“主方法在三场景、五方法下是否仍成立”。

建议脚本：
`experiments/run_readiness_multiseed.py`

建议场景：

1. `configs/paper_base.yaml`
2. `configs/paper_hard.yaml`
3. `configs/scenario_stress.yaml`

建议方法：

1. `periodic`
2. `security_risk`
3. `security_margin`
4. `rollout_joint`

建议 seeds：
`62-81`

建议命令：

```bash
PYTHONPATH=. python experiments/run_readiness_multiseed.py \
  --configs configs/paper_base.yaml configs/paper_hard.yaml configs/scenario_stress.yaml \
  --methods periodic security_risk security_margin rollout_joint \
  --num-seeds 20 \
  --seed-start 62 \
  --outdir results/scheme_c_readiness_20seed_2026-04-24_rollout_final
```

预期产物：

1. `all_runs.csv`
2. `summary.csv`
3. `summary_with_gains.csv`
4. `main_table.csv`
5. `main_table.md`

完成标准：

1. `paper_base / paper_hard / scenario_stress` 三场景齐全
2. 每个 `scenario × method` 的 `num_runs = 20`
3. `rollout_joint` 的最终结论可以和当前定向结果一致或更清楚

备注：
如果整包跑得太慢，继续沿用分块并行，然后合并。

---

## B. 主方法配对比较固化

优先级：`P0`

目的：
把“主方法是不是稳定更好”从口头判断变成正式统计结果。

建议脚本：

1. 直接读 `all_runs.csv` 做 paired 比较
2. 或使用 `experiments/run_scheme_c_pipeline.py` 自动生成

建议 baseline：

1. `periodic`
2. `security_risk`
3. `security_margin`

目标输出：

1. `mean_secrecy_gain`
2. `mean_outage_gain`
3. `paired_t_stat`
4. `paired_t_pvalue_approx`
5. `secrecy_win_seeds`

建议产物文件：
`paired_comparisons_rollout_joint.csv`

完成标准：

1. 至少对三场景都给出 `rollout_joint vs periodic`
2. 最终文档中能直接引用胜负 seed 数和平均增益

---

## C. 正式消融实验

优先级：`P0`

目的：
解释“主方法为什么有效”，而不是只给最终最好结果。

建议脚本：
`experiments/run_rollout_ablations.py`

当前脚本内置变体：

1. `rollout_joint`
2. `rollout_no_certificate`
3. `rollout_no_outage`
4. `rollout_no_pending_sync`
5. `rollout_one_step`

建议命令：

```bash
PYTHONPATH=. python experiments/run_rollout_ablations.py \
  --configs configs/paper_base.yaml configs/paper_hard.yaml configs/scenario_stress.yaml \
  --num-seeds 20 \
  --seed-start 62 \
  --outdir results/rollout_ablations_2026-04-24
```

预期产物：

1. `all_runs.csv`
2. `summary.csv`

建议补充的人工解释项：

1. `certificate` 项去掉后是否更激进
2. `outage` 项去掉后是否 secrecy 上去但 outage 变差
3. `pending_sync` 项去掉后是否出现同步抖动
4. `one_step` 是否验证 rollout horizon 的价值

完成标准：

1. 至少在 `paper_base` 和 `paper_hard` 上能指出 1-2 个真正关键模块
2. 能形成“方法机制解释”而不是只有性能表

备注：
如果后面要更深入，建议单独再补两个自定义消融：

1. 去掉 `AoI discount`
2. 去掉 `VOI bonus`

这两个目前不在脚本内置变体里，需要后续扩脚本。

---

## D. Runtime Tradeoff

优先级：`P1`

目的：
回答 reviewer 最容易问的问题：
“你的方法更强，但代价有多大？能不能调复杂度？”

建议脚本：
`experiments/run_runtime_tradeoff.py`

建议命令：

```bash
PYTHONPATH=. python experiments/run_runtime_tradeoff.py \
  --configs configs/paper_base.yaml configs/paper_hard.yaml \
  --seed 62 \
  --outdir results/runtime_tradeoff_2026-04-24
```

当前脚本内置变体：

1. `rollout_h1_b4`
2. `rollout_h1_b8`
3. `rollout_h2_b8`
4. `rollout_h2_b12`
5. `periodic`
6. `security_margin`

预期产物：
`runtime_tradeoff.csv`

完成标准：

1. 至少能画出 secrecy vs runtime 的 tradeoff
2. 能支撑“实际部署时可调 horizon / branching”的表述

---

## E. 随机扰动泛化实验

优先级：`P1`

目的：
证明主方法不是只对当前手调配置有效。

建议脚本：
`experiments/run_randomized_generalization.py`

脚本当前会随机扰动：

1. Eve 速度
2. Eve 噪声
3. 同步预算
4. 同步失败率
5. `r_min`
6. 用户位置

建议命令：

```bash
PYTHONPATH=. python experiments/run_randomized_generalization.py \
  --configs configs/paper_base.yaml configs/paper_hard.yaml configs/scenario_stress.yaml \
  --methods periodic security_margin rollout_joint \
  --num-perturbations 5 \
  --num-seeds 3 \
  --seed-start 90 \
  --outdir results/randomized_generalization_2026-04-24
```

预期产物：

1. `all_runs.csv`
2. `summary.csv`
3. `overall_summary.csv`

完成标准：

1. `rollout_joint` 在扰动后至少对 `periodic` 不显著崩掉
2. 能支持“方法对环境偏差有一定鲁棒性”的论述

---

## F. 最新口径 Holdout / Pipeline 文档同步

优先级：`P1`

目的：
把证书结果、主表结果、配对比较和文档输出统一到最新版本，避免论文里出现“主表新了，holdout 还是旧的”。

建议脚本：

1. `experiments/fit_certificate_holdout.py`
2. `experiments/run_scheme_c_pipeline.py`

建议路径风格：

1. holdout：
`results/scheme_c_holdout_2026-04-24_rollout_final`
2. readiness：
`results/scheme_c_readiness_20seed_2026-04-24_rollout_final`
3. small mdp：
`results/scheme_c_small_mdp_2026-04-24_rollout_final`
4. 文档：
`docs/scheme_c_results_2026-04-24_rollout_final.md`

建议完成标准：

1. holdout 配置和主表配置同版本
2. pipeline 自动生成的文档可直接给导师/审稿准备材料使用

---

## G. Toy 小场景理论上界

优先级：`P2`

目的：
提供一个“理论上限/上界 sanity-check”，哪怕只在小场景成立。

建议脚本：
`experiments/run_small_mdp_bound.py`

建议命令：

```bash
PYTHONPATH=. python experiments/run_small_mdp_bound.py \
  --config configs/small_mdp_bound.yaml \
  --seed 42 \
  --outdir results/small_mdp_bound_2026-04-24
```

预期产物：

1. `optimal_trace.csv`
2. `summary.csv`

完成标准：

1. 如果能跑通，正文可写“toy setting 下与理论最优的差距”
2. 如果跑不通，不影响主论文，只作为补充项

---

## H. Coupling / Threshold 机制分析图

优先级：`P2`

目的：
把“同步-孪生-保密”的耦合机制画成分析图，而不是只靠文字解释。

建议脚本：

1. `experiments/run_coupling.py`
2. `experiments/run_threshold.py`

适合生成的图：

1. `periodic_k vs avg_secrecy_rate`
2. `periodic_k vs outage_prob`
3. `avg_twin_badness vs avg_secrecy_rate`
4. `avg_sync_cost vs avg_secrecy_rate`
5. threshold scan 曲线

完成标准：

1. 正文里至少有 1-2 张机制图能支撑“为什么需要联合优化”

---

## 4. 推荐执行顺序

### 第 1 阶段：把主证据补齐

1. A. 最新主表重跑
2. B. 主方法配对比较固化
3. C. 正式消融实验

### 第 2 阶段：把论文说服力补硬

1. D. Runtime Tradeoff
2. E. 随机扰动泛化实验
3. F. 最新口径 Holdout / Pipeline 文档同步

### 第 3 阶段：把论文做丰满

1. G. Toy 小场景理论上界
2. H. Coupling / Threshold 机制分析图

---

## 5. 建议的最终论文证据包

如果时间有限，最少要凑齐下面这 6 份结果：

1. 最新主表 `main_table.csv`
2. `paired_comparisons_rollout_joint.csv`
3. `rollout_ablations` 汇总表
4. `runtime_tradeoff.csv`
5. `randomized_generalization` 汇总表
6. 最新 holdout `holdout_summary.csv`

如果这 6 项都齐了，论文就不再只是“主方法好像好一点”，而是：

1. 有主结果
2. 有统计稳定性
3. 有机制解释
4. 有复杂度代价说明
5. 有泛化鲁棒性
6. 有证书线支撑

---

## 6. 当前建议

下一步最推荐直接执行：

1. 先跑 A：最新完整主表
2. 接着跑 C：正式消融
3. 再跑 D：runtime tradeoff

原因：
这三项最直接决定“论文主干是否已经站住”。
